// use std::{
//     path::PathBuf,
// };

// use std::{
//     // cmp,
//     // collections::HashSet,
//     // iter::FromIterator,
// };

use crate::{
    solver,
    problem,
};

#[allow(dead_code)]
pub struct DancerSolver {
    solver: solver::Solver,
}

impl DancerSolver {
    pub fn new(solver: solver::Solver) -> DancerSolver {
        DancerSolver {
            solver,
        }
    }

    pub fn solve(&self) {
    }

    pub fn place_vertex(&self) {
    }
}
